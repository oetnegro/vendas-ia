# Gemini Developer API key (same key the TS app uses as GEMINI_API_KEY).
GEMINI_API_KEY=
# Optional model override (default: gemini-2.5-flash).
GEMINI_MODEL=gemini-2.5-flash
# Shared secret the TS engine sends in the X-ADK-Secret header. Required to serve.
ADK_SHARED_SECRET=
